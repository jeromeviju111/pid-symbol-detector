# --- Web UI ---
gradio>=4.0

# --- Symbol detection (YOLO) ---
ultralytics>=8.0
torch>=2.0
torchvision>=0.15

# --- Image processing ---
opencv-python>=4.8
numpy>=1.24

# --- OCR (tag reading) ---
easyocr>=1.7

# --- PDF -> image conversion ---
PyMuPDF>=1.23

# --- Tables (symbol/hierarchy summaries) ---
pandas>=2.0

# --- data.yaml parsing (evaluate_pipeline.py) ---
PyYAML>=6.0

# --- LLM chat calls (diagram Q&A) — Ollama is local/free, OpenRouter is
# --- hosted/paid; requests handles the HTTP for both (see llm_query.py) ---
requests>=2.31

# --- evaluate_pipeline.py OCR accuracy metrics only — not needed to run the app itself ---
jiwer>=3.0

# sqlite3 (corrections.db), re, and everything else the code imports
# (os, json, pathlib, traceback, datetime, collections, glob) are all
# part of Python's standard library — no separate install needed.
