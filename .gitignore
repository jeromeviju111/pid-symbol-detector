venv/
__pycache__/
*.pyc
.DS_Store
pdf_pages/
*_resized.png

# Gradio's public-share certificate — regenerated per run, machine-specific
.gradio/

# YOLO evaluation output — regenerated by evaluate_pipeline.py, not source
runs/

# Local correction data — see note below on why this stays out of git
corrections.db

# Never commit API keys (OPENROUTER_API_KEY is read from the environment
# in llm_query.py precisely so it's never hardcoded/committed here)
.env
